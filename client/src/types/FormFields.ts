export type LoginFormFields = {
  email: string;
  password: string;
};

export type CreateProjectFormFields = {
  name: string;
  description: string;
};

export type RegisterFormFields = {
  name: string;
  email: string;
  password: string;
};
